# CLAUDE.md

이 저장소는 개인 AI 엔지니어링 포트폴리오 + 개발 허브입니다. 작업 시 아래 원칙을 따르세요.

## 저장소 목적 (우선순위 순)

1. **AI 엔지니어 포트폴리오** — `main`은 외부에 보여줄 수 있는 상태를 유지
2. **개인 개발 허브** — 실험/학습/프로토타입 진행
3. **회사 이관** — 여기서 검증된 코드를 회사 프로젝트로 옮김

## 구조 규칙

- 새 프로젝트: `projects/<프로젝트명>/` 에 생성, 자체 README.md 필수
- 실험/POC: `experiments/<주제명>/` — 완성도 요구 없음, 검증되면 projects/로 승격
- 재사용 모듈·보일러플레이트: `templates/` — 회사 이관 후보는 여기에 정리
- 문서·학습 노트: `docs/`

## 작업 방식

- 작업 단위마다 Orca 워크트리(`orca worktree create --name <작업명>`)에서 브랜치로 진행하고, 완료 후 `main`에 머지
- `main` 직접 작업은 문서 수정 등 사소한 변경만
- 커밋 메시지는 한국어/영어 무관, 변경 의도가 드러나게 작성

## 협업 지침 (에이전트 공통)

- 사용자와의 대화·보고는 **한국어**로 한다
- **머지 게이트**: 워커/에이전트 작업 결과는 자동으로 main에 머지하지 않는다. diff 요약을 보여주고 사용자 승인 후 머지한다
- 병렬 작업은 **파일이 겹치지 않는 독립 작업**만 나눈다. 겹치면 순차 진행
- 작업이 끝나 머지(또는 파기)된 워크트리와 브랜치는 즉시 정리한다
- 워크트리/브랜치 이름은 작업 내용이 드러나는 kebab-case (예: `readme-polish`)
- 커밋 계정은 `junki7853` (git config). Orca 워커 브랜치의 `tk101-ai/` 접두사는 Orca 관리 계정 이름일 뿐이며 GitHub에 푸시하지 않는다

## 금지 사항

- 회사 내부 정보(사내 URL, 고객 데이터, 계정 정보) 커밋 금지 — 이 저장소는 공개 포트폴리오 전제
- API 키·비밀번호는 `.env`로만 관리 (`.gitignore`에 포함됨), 예시는 `.env.example`로 제공

## 회사 이관 시 체크리스트

- [ ] 의존성 버전 명시 (requirements.txt / package.json 잠금)
- [ ] 하드코딩된 개인 경로·설정 제거, 환경변수화
- [ ] README에 설치·실행 방법 문서화
- [ ] 라이선스 확인 (외부 라이브러리 포함)
