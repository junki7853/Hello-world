# CLAUDE.md

이 저장소는 개인 AI 엔지니어링 포트폴리오 + 개발 허브입니다. 작업 시 아래 원칙을 따르세요.

## 저장소 목적 (우선순위 순)

1. **AI 엔지니어 포트폴리오** — `main`은 외부에 보여줄 수 있는 상태를 유지
2. **개인 개발 허브** — 실험/학습/프로토타입 진행
3. **회사 이관** — 여기서 검증된 코드를 회사 프로젝트로 옮김

## 구조 규칙

- 새 프로젝트: `projects/<프로젝트명>/` 에 생성, 자체 README.md 필수
- 실험/POC: `experiments/<주제명>/` — 완성도 요구 없음, 검증되면 projects/로 승격
- 재사용 모듈·보일러플레이트: `templates/` — 회사 이관 후보는 여기에 정리
- 문서·학습 노트: `docs/`

## 작업 트랙 (규모·성격에 따라 선택)

- **경량**: 오타·문서·설정 등 즉시 끝나는 변경 → main 직접 커밋, 파이프라인 생략
- **표준 (신규 개발)**: 새 기능·새 프로젝트 → 아래 6단계 전체
- **개선/수정**: 기존 코드의 변경·버그픽스·리팩터링 → 6단계를 따르되 1단계를 "영향 범위 분석"(호출자·의존성 추적, 회귀 위험 파악)으로 진행하고, 5단계에서 기존 기능 회귀 확인을 반드시 포함

## 표준 작업 파이프라인 (모든 개발 작업에 자동 적용)

사용자가 "~를 만들 거야"라고 하면 별도 지시 없이도 아래 6단계를 순서대로 진행한다:

1. **개발 계획 수립** — `plan` 스킬로 계획 수립 (대형 기능·정밀 코드베이스 분석이 필요하면 `prp-plan`), 사용자 승인 후 Orca 워크트리 생성
2. **개발 진행** — 관련 개발 스킬을 적극 활용: coding-standards(공통) · python-patterns/python-testing(Python) · backend-patterns/api-design(서버·API) · frontend-patterns(웹 UI) · mcp-server-patterns(MCP) · git-workflow(브랜치·커밋)
3. **코드리뷰** — 정확성·버그·보안 중심 리뷰 (`/code-review`)
4. **포니테일 리뷰** — `ponytail-review`로 과잉설계·군살 제거 리뷰, 지적사항 반영
5. **스테이징 검증** — 테스트 전체 실행 + 실제 동작 확인 (개선/수정 트랙은 기존 기능 회귀 확인 포함). 배포 인프라가 생기면 스테이징 서버 검증으로 확장
6. **머지 & 배포** — diff 요약 → 사용자 승인 → main 머지 (+ 배포 대상이면 배포) → **워크트리·브랜치 자동 삭제(확인 불필요)** → `docs/WORKLOG.md`에 기록

## 멀티 에이전트 운용

- **파일을 수정하는 병렬 개발**: Orca 오케스트레이션(워크트리 분리)이 기본 — run-create → task-create → worker-start → 코디네이터 감독
- **조사·리뷰·분석 등 읽기 병렬**: 코디네이터의 내장 서브에이전트(Agent 도구) 사용 — 파일 수정이 없어 워크트리 불필요, 더 가볍다
- ECC의 멀티에이전트 하네스(dmux-workflows, claude-devfleet, autonomous-agent-harness 등)는 Orca와 역할이 중복되므로 도입하지 않는다
- 병렬 분해는 파일이 겹치지 않는 독립 작업만. 겹치면 순차 진행

## 문서화 (임시 — 로컬 허브 보류 중)

- 작업 완료/파기 시 `docs/WORKLOG.md`에 1건 기록: 날짜 / 작업명 / 트랙 / 상태 / 요약(교훈 포함) / 커밋
- 기술·설계 문서는 `docs/`에 마크다운으로 작성
- 로컬호스트 프로젝트 허브(웹 대시보드)는 보류 상태 — 재개 시 WORKLOG 데이터를 이전한다

## 작업 방식

- 작업 단위마다 Orca 워크트리(`orca worktree create --name <작업명>`)에서 브랜치로 진행하고, 완료 후 `main`에 머지
- `main` 직접 작업은 문서 수정 등 사소한 변경만
- 커밋 메시지는 한국어/영어 무관, 변경 의도가 드러나게 작성

## 협업 지침 (에이전트 공통)

- 사용자와의 대화·보고는 **한국어**로 한다
- **머지 게이트**: 워커/에이전트 작업 결과는 자동으로 main에 머지하지 않는다. diff 요약을 보여주고 사용자 승인 후 머지한다
- 병렬 작업은 **파일이 겹치지 않는 독립 작업**만 나눈다. 겹치면 순차 진행
- 작업이 끝나 머지(또는 파기)된 워크트리와 브랜치는 즉시 정리한다
- 워크트리/브랜치 이름은 작업 내용이 드러나는 kebab-case (예: `readme-polish`)
- 커밋 계정은 `junki7853` (git config). Orca 워커 브랜치의 `tk101-ai/` 접두사는 Orca 관리 계정 이름일 뿐이며 GitHub에 푸시하지 않는다

## 금지 사항

- 회사 내부 정보(사내 URL, 고객 데이터, 계정 정보) 커밋 금지 — 이 저장소는 공개 포트폴리오 전제
- API 키·비밀번호는 `.env`로만 관리 (`.gitignore`에 포함됨), 예시는 `.env.example`로 제공

## 회사 이관 시 체크리스트

- [ ] 의존성 버전 명시 (requirements.txt / package.json 잠금)
- [ ] 하드코딩된 개인 경로·설정 제거, 환경변수화
- [ ] README에 설치·실행 방법 문서화
- [ ] 라이선스 확인 (외부 라이브러리 포함)
